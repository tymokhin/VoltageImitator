#include "main.h"
#include <math.h>
#include <string.h>
#include <stdbool.h>

/* UART обробка параметрів */
void handle_uart_command(uint8_t *data, size_t len) {
    // Реалізуйте парсинг параметрів A, alpha, phi, freq для кожного каналу
    // Наприклад, по 16 байт на канал (float x4)
}
