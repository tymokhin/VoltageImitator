/*
 * hrtim.c
 *
 *  Created on: May 10, 2025
 *      Author: Oleksandr
 *
 * Тут раніше лежав каркас під burst-DMA оновлення CMPx і подвійну
 * буферизацію точок PWM. Від нього відмовились: у поточній архітектурі
 * compare пишеться напряму з такту контуру керування (wave_generator_task.c),
 * бо зворотний зв'язок вимагає перерахунку кожного такту, а не програвання
 * заздалегідь порахованого буфера.
 *
 * Разом із каркасом прибрані статичні буфери pwm_buffer[2][4][128] і
 * supporting-масиви - вони займали ~2.1 КБ з 12 КБ SRAM і ніде не читались.
 * Посилання на hdma_hrtim1_a теж прибране: такого handle в проєкті немає.
 */

#include "main.h"
#include "hrtim.h"
